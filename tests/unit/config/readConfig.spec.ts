describe('config/readConfig', () => {

    it('Uses readFile()')

    it('Uses parse()')

    it('Returns error from readFile()')

    it('Returns error from parse()')

    it('Returns result from parse() when no error')

})